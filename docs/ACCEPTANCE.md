# چک‌لیست پذیرش MVP

- [ ] کانال اجباری سراسری از پنل اضافه می‌شود و اگر ربات ادمین نباشد رد می‌گردد
- [ ] برگزارکننده کانال را وصل و مالکیت را ثابت می‌کند
- [ ] کاستوم با زمان، ظرفیت، کانال شرطی و دعوت ساخته می‌شود
- [ ] مدیر تأیید می‌کند و در فهرست عمومی می‌آید
- [ ] بازیکن از `event_TOKEN` وارد می‌شود
- [ ] عضویت و Referral در چک‌لیست دیده می‌شود
- [ ] ثبت‌نام همزمان از ظرفیت عبور نمی‌کند
- [ ] خروج از کانال قبل از ارسال رمز، کاربر را فاقد شرایط می‌کند
- [ ] رمز فقط یک‌بار به واجدین در خصوصی می‌رسد
- [ ] Delivery و Audit در پنل دیده می‌شود
- [ ] Ban اعمال می‌شود
- [ ] لغو/تغییر زمان Job را به‌روز می‌کند
- [ ] `pytest -q` سبز است
