# Bot-only stack for low-RAM VPS (no panel/nginx).
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.minimal.yml up -d postgres redis migrate api
services:
  frontend:
    profiles: ["panel"]
  nginx:
    profiles: ["panel"]
