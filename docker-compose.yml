# Docker's default json-file driver never rotates. On a host shared with other
# stacks an unbounded log fills the disk and takes every container down with
# it, so every service here caps its own logs.
x-logging: &logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-python-env: &python-env
  MALLOC_ARENA_MAX: "1"
  PYTHONMALLOC: malloc
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"
  BOT_EMBEDDED: "true"
  DEBUG: "false"
  LOG_LEVEL: WARNING
  JOB_DISPATCH_INTERVAL_SECONDS: "60"

services:
  postgres:
    logging: *logging
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ffroom}
      POSTGRES_USER: ${POSTGRES_USER:-ffroom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me-db-password}
    command:
      - postgres
      - -c
      - shared_buffers=16MB
      - -c
      - work_mem=1MB
      - -c
      - maintenance_work_mem=16MB
      - -c
      - effective_cache_size=48MB
      - -c
      - max_connections=20
      - -c
      - wal_buffers=1MB
    mem_limit: 64m
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ffroom} -d ${POSTGRES_DB:-ffroom}"]
      interval: 30s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    logging: *logging
    image: redis:7-alpine
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --maxmemory
      - 12mb
      - --maxmemory-policy
      - allkeys-lru
      - --save
      - ""
    mem_limit: 20m
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  migrate:
    logging: *logging
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    environment:
      <<: *python-env
    command:
      - sh
      - -c
      - |
        set -e
        echo "migrate: alembic current"
        alembic current
        echo "migrate: alembic upgrade"
        alembic upgrade head
        echo "migrate: bootstrap"
        python -m app.cli.main bootstrap
        echo "migrate: done"
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: 256m
    restart: "no"

  api:
    logging: *logging
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    environment:
      <<: *python-env
    mem_limit: 384m
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --workers
      - "1"
      - --log-level
      - warning
      - --no-access-log
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "/usr/bin/curl", "-fsS", "--max-time", "3", "http://127.0.0.1:8000/health/live"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  bot:
    logging: *logging
    profiles: ["bot"]
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    environment:
      <<: *python-env
      BOT_EMBEDDED: "false"
      TELEGRAM_MODE: polling
      LOG_LEVEL: INFO
    # Measured: ~87MB of Python objects for the dispatcher alone, 1591 modules,
    # ~126MB resident once polling. 256m killed it during startup with no swap
    # cushion, so the ceiling is set well clear of the real figure rather than
    # just above it. Still bounded, so a leak cannot take the host down.
    mem_limit: 512m
    memswap_limit: 512m
    cpus: 1.0
    command: ["python", "-m", "app.bot.runner"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  frontend:
    logging: *logging
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${API_BASE_URL:-http://localhost:8080/api}
    env_file: .env
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${API_BASE_URL:-http://localhost:8080/api}
      NODE_OPTIONS: --max-old-space-size=48
    mem_limit: 64m
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  nginx:
    logging: *logging
    image: nginx:1.27-alpine
    mem_limit: 12m
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      # HTTP_PORT lets the panel coexist with another site already on :80
      - "${HTTP_PORT:-80}:80"
    depends_on:
      - api
      - frontend
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
